<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools" android:layout_width="match_parent"
    android:layout_height="match_parent" android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin" tools:context=".MainActivity"
    android:weightSum="1"
    android:orientation="vertical"
    android:background="@drawable/logo2">



        <!--<Button-->
            <!--android:id="@+id/btnPlay"-->
            <!--android:layout_width="160dp"-->
            <!--android:layout_height="160dp"-->
            <!--android:background="@drawable/button"-->
            <!--android:gravity="center_vertical"-->
            <!--android:onClick="changeToPlay"-->
            <!--android:layout_alignParentBottom="true"-->
            <!--android:layout_alignParentRight="true"-->
            <!--android:layout_alignParentEnd="false"-->
            <!--android:layout_marginBottom="145dp" />-->

    <ImageButton
        style="@style/buttonStyle"
        android:id="@+id/btnVicons"
        android:background="@drawable/vicons"
        android:onClick="changeToPlay"
        android:layout_above="@+id/btnSettings"
        android:layout_alignLeft="@+id/btnSettings"
        android:layout_alignStart="@+id/btnSettings" />
    <ImageButton
        style="@style/buttonStyle"
        android:id="@+id/btnPlay"
        android:background="@drawable/play_game"
        android:onClick="changeToPlay"
        android:layout_alignBottom="@+id/btnVicons"
        android:layout_alignParentRight="true"
        android:layout_alignParentEnd="true" />
    <ImageButton
        style="@style/buttonStyle"
        android:id="@+id/btnSettings"
        android:background="@drawable/settings"
        android:onClick="changeToSettings"
        android:layout_alignParentBottom="true"
        android:layout_alignParentLeft="true"
        android:layout_alignParentStart="true" />
    <ImageButton
        style="@style/buttonStyle"
        android:id="@+id/btnShare"
        android:background="@drawable/share"
        android:onClick="changeToResult"
        android:layout_alignParentBottom="true"
        android:layout_alignRight="@+id/btnPlay"
        android:layout_alignEnd="@+id/btnPlay" />


</RelativeLayout>
